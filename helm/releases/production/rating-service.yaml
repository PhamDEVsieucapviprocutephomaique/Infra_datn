# environments/production/rating-service.yaml

namespace: production
replicas: 2

image:
  repository: myregistry.azurecr.io/rating-service
  tag: v1.2.0
  pullPolicy: Always

service:
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  host: api.rating.example.com
  annotations:
    kubernetes.io/ingress.class: nginx
  tls:
    enabled: true
    secretName: rating-tls

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 8
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

pdb:
  enabled: true
  minAvailable: 1

serviceAccount:
  create: true
  annotations: {}

configmap:
  enabled: true
  data:
    DATABASE_URL: "postgresql://rating:pass@postgres-rating.prod:5432/ratingdb"
    REDIS_URL: "redis.prod:6379"
    LOG_LEVEL: "info"

env: {}

healthCheck:
  path: /health
  initialDelaySeconds: 30
  periodSeconds: 10
